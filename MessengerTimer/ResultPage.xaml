<Page x:Class="MessengerTimer.ResultPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:MessengerTimer"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:data="using:TimeData" mc:Ignorable="d">
    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="ResultText">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Margin" Value="10 0 10 0" />
            <Setter Property="Width" Value="70" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>

        <Style TargetType="StackPanel" x:Key="ResultPageStackPanel">
            <Setter Property="Orientation" Value="Horizontal" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="10 0 10 0" />
        </Style>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Style="{StaticResource ResultPageStackPanel}">
            <TextBlock Text="Id" Style="{StaticResource ResultText}" Width="30" />
            <TextBlock Text="Result" Style="{StaticResource ResultText}" />
            <TextBlock Text="Ao5" Style="{StaticResource ResultText}" />
            <TextBlock Text="Ao12" Style="{StaticResource ResultText}" />
        </StackPanel>

        <Canvas Width="2000" Height="2" Grid.Row="1">
            <Line X1="0" Y1="0" X2="2000" Y2="0" Stroke="Black" StrokeThickness="2" />
        </Canvas>

        <ListView Name="ResultListView" ItemsSource="{x:Bind Results}" Grid.Row="2" MinHeight="600" MaxHeight="900">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Result">
                    <StackPanel Style="{StaticResource ResultPageStackPanel}" Margin="0 0 15 0">
                        <TextBlock Name="ResultIdTextBlock" Text="{x:Bind Id }" Style="{StaticResource ResultText}" Width="30" />
                        <TextBlock Name="ResultTextBlock" Text="{x:Bind resultValue}" Style="{StaticResource ResultText}" />
                        <TextBlock Name="ResultAo5TextBlock" Text="{x:Bind ao5Value}" Style="{StaticResource ResultText}" />
                        <TextBlock Name="ResultAo12TextBlock" Text="{x:Bind ao12Value}" Style="{StaticResource ResultText}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
